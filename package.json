{
  "name": "kaseya-gcal-sync",
  "version": "1.0.0",
  "description": "Scrapes Kaseya Center events and syncs them to Google Calendar",
  "main": "src/sync.js",
  "scripts": {
    "start": "node src/sync.js",
    "scrape": "node -e \"require('./src/scraper').scrapeEvents().then(e => console.log(JSON.stringify(e, null, 2)))\"",
    "debug": "node -e \"require('./src/scraper').scrapeEvents().then(e => { console.log('Found', e.length, 'events'); e.forEach(ev => console.log('-', ev.name, '|', ev.dateText, ev.timeText, '|', ev.url)); })\""
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "ical-generator": "^10.0.0",
    "playwright": "^1.58.2"
  }
}
